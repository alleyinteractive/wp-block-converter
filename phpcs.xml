<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="wp-block-converter" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/squizlabs/PHP_CodeSniffer/master/phpcs.xsd">
  <description>PHP_CodeSniffer standard for wp-block-converter.</description>

  <!-- Include Alley Rules -->
  <rule ref="Alley-Interactive" />

  <!-- This library uses json_encode() directly (e.g. to serialize block attributes) rather than wp_json_encode(), since its output must exactly match core's block serialization and doesn't need wp_json_encode()'s escaping/filtering behavior. -->
  <rule ref="WordPress.WP.AlternativeFunctions">
    <exclude name="WordPress.WP.AlternativeFunctions.json_encode_json_encode" />
  </rule>

  <!-- Filenames follow PSR-4 (matching the class/trait name) rather than the WordPress class-*.php convention. -->
  <rule ref="WordPress.Files.FileName">
    <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase" />
    <exclude name="WordPress.Files.FileName.InvalidClassFileName" />
  </rule>

  <!--
  Pass some flags to PHPCS:
    p flag: Show progress of the run.
    s flag: Show sniff codes in all reports.
  -->
  <arg value="ps" />

  <!-- Whenever possible, cache the scan results and re-use those for unchanged files on the next scan. -->
  <arg name="cache" value=".phpcs.cache.json" />

  <!-- Strip the filepaths down to the relevant bit. -->
  <arg name="basepath" value="./" />

  <!-- Check up to 20 files simultaneously. -->
  <arg name="parallel" value="20" />

  <!-- Set severity to 1 to see everything that isn't effectively turned off. -->
  <arg name="severity" value="1" />

  <!-- Exclude a few directories and autogenerated files. -->
  <exclude-pattern>tests/</exclude-pattern>
  <exclude-pattern>build/</exclude-pattern>
  <exclude-pattern>vendor/</exclude-pattern>

  <!-- The version set here matches the minimum version tested in CI. -->
  <config name="minimum_supported_wp_version" value="7.0" />

  <!-- Define the prefixes that can be used by the plugin -->
  <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
    <properties>
      <property name="prefixes" type="array">
        <element value="alley" />
        <element value="wp_block_converter" />
      </property>
    </properties>
  </rule>
</ruleset>
